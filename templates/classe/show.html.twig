{% extends 'base.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}
    <h1>Classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ classe.niveau }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_classe_index') }}" class="btn btn-primary">
    <i class="fas fa-arrow-left"></i></a>

    <a href="{{ path('app_classe_edit', {'id': classe.id}) }}"><button type="button" class="btn btn-primary">
    <i class="fas fa-edit"></i> </a>

    {{ include('classe/_delete_form.html.twig') }}
{% endblock %}
