{% extends 'base.html.twig' %}


{% block title %}Faculte{% endblock %}

{% block body %}
    <h1>Faculte</h1>

    <table id="myTable" class="display">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ faculte.id }}</td>
            </tr>
            <tr>
                <th>Nomf</th>
                <td>{{ faculte.nomf }}</td>
            </tr>
            <tr>
                <th>Coded</th>
                <td>{{ faculte.coded }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_faculte_index') }}"class="btn btn-primary">
    <i class="fas fa-arrow-left"></i></a>

    <a href="{{ path('app_faculte_edit', {'id': faculte.id}) }}"><button type="button" class="btn btn-primary">
    <i class="fas fa-edit"></i> </a>

    {{ include('faculte/_delete_form.html.twig') }}
{% endblock %}
